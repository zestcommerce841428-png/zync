# Tiny image for the PeerJS signaling server (bin/peerjs.js).
# Installs only express + peer (matching the app's versions) — no app build.
FROM node:lts-alpine
WORKDIR /app
RUN npm install --omit=dev express@^5 peer@^1
COPY bin/peerjs.js ./peerjs.js
EXPOSE 9000
CMD ["node", "peerjs.js"]
