# Screenshots

Drop PNGs here to populate the README gallery:

- `landing.png` — the marketing landing page
- `send.png` — the "Send a file" transfer screen
- `tools.png` — the in-browser tools grid

Recommended width ~1280px. Until you add them, the README image links show as
broken placeholders (expected).
